type AppConfig = {
    app_name: string,
    user_name: string,
    user_id: number
}

const config: AppConfig = {
    app_name: "Blake's Personal Dashboard",
    user_name: "Blake Runyon",
    user_id: 1
}

export default config;